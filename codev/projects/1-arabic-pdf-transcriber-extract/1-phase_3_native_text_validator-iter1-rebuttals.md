# Phase 3 (Native-Text Validator) — Iteration 1 Rebuttals

## Codex (REQUEST_CHANGES) — addressed

| Codex point | Action | Where |
|---|---|---|
| `validate_page()` accepts pages whose native extraction is effectively empty (whitespace-only `has_text_layer=True` pages return `accept=True`) — violates the phase objective of rejecting "missing or insufficient" native output | Added `empty_text_layer` reject reason. Fires when `letter_count == 0` *and* `replacement_ratio <= max_replacement_ratio`. Ordering matters: the replacement-glyph signal runs first so a page made entirely of `■` / PUA codepoints gets the more specific, actionable reason rather than the generic "no letters" one. New regression tests (`test_validate_page_empty_text_layer_rejected`, `test_validate_page_punctuation_only_rejected`) cover both paths. | `src/arabic_pdf_transcribe/validate/native_validator.py` (`validate_page`); `tests/test_native_validator.py`. |
| Plan deliverable requires the two new `digital-broken/` fixtures to have documented licenses; `LICENSES.md` only listed `digital-clean/` rows | Added rows for `digital-broken/mojibake.pdf` and `digital-broken/replacement-glyphs.pdf` with provenance (generated by `tools/generate_fixtures.py`), the failure mode they exercise, and license (MIT, project-owned). | `tests/fixtures/pdfs/LICENSES.md`. |

**Disagreements with Codex**: none.

## Claude (APPROVE w/ comments) — addressed

| Claude point | Action | Where |
|---|---|---|
| `page: Any` violates CLAUDE.md "NEVER use `Any`" rule (used to avoid validate→extract circular import) | Replaced with `from __future__ import annotations` + `if TYPE_CHECKING: from arabic_pdf_transcribe.extract.native import NativePage`. Plan-signature `page: NativePage` is restored at the type level; runtime keeps the duck-typed `getattr` path so the import does not become load-bearing. | `src/arabic_pdf_transcribe/validate/native_validator.py`. |
| `LICENSES.md` missing rows for the broken fixtures (same as codex point 2) | See codex row above. | `tests/fixtures/pdfs/LICENSES.md`. |
| Mojibake fixture uses `?` (U+003F) — exercises only the KL signal path, not realistic multi-signal rejection | Acknowledged. Phase 9 corpus expansion will replace synthetic broken fixtures with real broken-text-layer PDFs that exercise multiple signal paths simultaneously. The current fixture pair (mojibake + replacement-glyphs) covers two of the three signal axes (KL divergence + replacement-glyph ratio); the Arabic-ratio path is exercised by the synthetic-page tests `test_validate_page_arabic_storm_swapped_to_latin_rejected` and `test_validate_page_short_arabic_with_drop_in_ratio_passes`. Documented as a known phase-3 limitation in the rebuttals; not blocking. | (no code change — phase-9 work item.) |

**Disagreements with Claude**: none.

## Gemini Pro — SKIPPED (upstream infrastructure)

Sixth quota-exhaustion in this project. Skipped per architect rule
[2026-05-01T18:32:24.195Z]. See
`1-phase_3_native_text_validator-iter1-gemini.txt`.

## Verification after revisions

- `make lint` clean (ruff check + format check)
- `make audit` `license_audit: OK`
- `make test` **94 tests pass**, 93% coverage
  - New: `test_validate_page_empty_text_layer_rejected`
  - New: `test_validate_page_punctuation_only_rejected`
  - Two threshold-bite tests refactored from fixture-based to
    synthetic-page so the new `empty_text_layer` short-circuit does not
    pre-empt the threshold check they are asserting.

## Summary

- Codex `REQUEST_CHANGES` → addressed in full (2/2 issues).
- Claude `APPROVE w/ comments` → addressed (2 of 3 in code; the 3rd
  deferred to phase 9 with documented rationale, matching how Claude
  framed it as non-blocking).
- Gemini → skipped per architect instruction.
